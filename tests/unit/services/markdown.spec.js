describe('Markdown Service', () => {
  describe('when rendering markdown', () => {
    describe('when highlighting code', () => {
      describe('when language is specified and is imported', () => {
        test('it should syntax highlight for that specific language', () => {

        });
      });
      describe('when lanugage isnt specified', () => {
        test('it should render the escaped text', () => {

        });
      });
    });
  });
});
