describe('CMS Service', () => {
  describe('when getting a collection', () => {
    describe('when getting a single resource', () => {
      test('it should get a single resource by its id', () => {});
    });

    describe('when requesting published versions of a resource', () => {
      test('it should get published versions of a resource', () => {});
    });

    describe('when filtering in other ways', () => {
      test('it should filter the resource based on these additional fields', () => {});
    });

    describe('when getting all resources', () => {
      test('it should get all the resources of that type', () => {

      });
    });
  });

  describe('when getting a singleton', () => {
    test('it should fetch the singleton', () => {

    });
  });
});
