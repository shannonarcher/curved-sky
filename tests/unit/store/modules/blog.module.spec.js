describe('Blog Vuex Module', () => {
    describe('when getting the blog entries', () => {
        test('it should sort the posts by created date', () => {

        });

        test('it should format the posts correctly', () => {

        });
    });

    describe('when getting an entry', () => {
        describe('when an existing entry exists', () => {
            test('it should return the existing entry', () => {

            });
        });

        describe('when no entry exists', () => {
            test('it should get all the entries', () => {

            });

            describe('when a matching entry is found', () => {
                test('it should add the entry to the store', () => {

                });
            });
        });
    });

    describe('when getting entries', () => {

    });

    describe('when refreshing entries', () => {

    });
});